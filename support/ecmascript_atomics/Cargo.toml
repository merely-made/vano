# THIS FILE IS AUTOMATICALLY GENERATED BY CARGO
#
# When uploading crates to the registry Cargo will automatically
# "normalize" Cargo.toml files for maximal compatibility
# with all versions of Cargo and also rewrite `path` dependencies
# to registry (e.g., crates.io) dependencies.
#
# If you are reading this file be aware that the original Cargo.toml
# will likely look very different (and much more reasonable).
# See Cargo.toml.orig for the original contents.

[package]
edition = "2024"
name = "ecmascript_atomics"
version = "0.2.3"
authors = ["Aapo Alasuutari"]
build = false
autolib = false
autobins = false
autoexamples = false
autotests = false
autobenches = false
description = "ECMAScript and Java memory model racy atomics."
readme = "README.md"
keywords = [
    "ecmascript",
    "java",
    "atomics",
]
categories = ["concurrency"]
license = "MPL-2.0"
repository = "https://github.com/trynova/ecmascript_atomics/tree/main/ecmascript_atomics"
resolver = "2"

[features]
alloc = []
# Genet's first wasm64 content worker is deliberately single-threaded and does
# not expose SharedArrayBuffer or Atomics. This backend preserves ordinary
# ArrayBuffer/TypedArray storage without pretending to implement ECMAScript's
# racy shared-memory model.
single-threaded = []

[lib]
name = "ecmascript_atomics"
path = "src/lib.rs"

[[test]]
name = "api"
path = "tests/api.rs"

[dependencies.ecmascript_atomics_gen_copy]
version = "0.1.0"
